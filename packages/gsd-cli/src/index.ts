/**
 * @gsd-build/gsd-cli — GSD standalone CLI entry point.
 *
 * Wires gsd-core orchestration with gsd-tui for output
 * and dispatches work via harness adapters.
 */

export { run } from "./run.ts";
